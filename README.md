# Noom

Shopper-only demo marketplace MVP.

For setup, architecture, and agent instructions, see **[AGENTS.md](./AGENTS.md)**.

Quick start:

```bash
docker compose up -d
pnpm install
pnpm db:push
pnpm db:seed
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000).
